interface:
  display_name: "Stash"
  short_description: "Search and safely manage inactive Agent Skills"
  default_prompt: "Use $stash to find, update, or explicitly manage a skill in the inactive local store."

policy:
  allow_implicit_invocation: false
