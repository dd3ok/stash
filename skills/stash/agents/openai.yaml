interface:
  display_name: "Stash"
  short_description: "Search and manage inactive local Agent Skills"
  default_prompt: "Use $stash to find a stored skill or explicitly manage a standalone skill in the inactive local store."

policy:
  allow_implicit_invocation: false
