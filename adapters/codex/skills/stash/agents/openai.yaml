interface:
  display_name: "Agent Skills Stash"
  short_description: "Open local Agent Skills only when requested"
  default_prompt: "Use $stash design-system to load that stored skill, or ask $stash to find every stored skill relevant to a task."

policy:
  allow_implicit_invocation: false
